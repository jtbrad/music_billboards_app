<h1>Artists</h1>

<div class="ui list">
  <% @artists.each do |artist| %>
    <div class="item">
      <%= link_to "#{artist.first_name} #{artist.last_name}", artist_path(artist) %>
    </div>
  <% end %>
</div>